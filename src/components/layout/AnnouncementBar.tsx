
export function AnnouncementBar() {
  return (
    <div
      className="w-screen py-4 overflow-hidden wave-pattern"
      aria-label="Decorative wave banner"
    >
      {/* The background is the content */}
    </div>
  );
}
